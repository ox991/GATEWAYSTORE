version: '2'
services:
  gatewaystore-postgresql:
    image: postgres:12.1
    # volumes:
    #     - ~/volumes/jhipster/gatewaystore/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=gatewaystore
      - POSTGRES_PASSWORD=
    ports:
      - 5432:5432
